#include<iostream>
using namespace std;

int main()
{
	int tc;
	cin>>tc;
	for(int test=1;test<=tc;test++)
	{
		int a,b,sum=0;
		cin>>a>>b;
		if(a%2==0)
		a++;
		if(b%2==0)
		b--;
		
		for(int i=a;i<=b;i+=2)
		{
			sum+=i;
		}
		cout<<"Case "<<test<<": "<<sum<<endl;
	}
}
